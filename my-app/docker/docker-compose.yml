version: '3.8'

services:
  web:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    container_name: nextjs_portal
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=https://api.example.com
    restart: always
